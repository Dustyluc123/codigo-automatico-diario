
[DOC] - Tarefa Concluída:## Roadmap do Projeto

### Frontend
- [ ] Criar a estrutura básica do `index.html`.
- [x] Criar o arquivo `style.css` e vincular ao `index.html` dentro da tag `<head>`. Adicionar regras de CSS para estilizar a classe `product-list`, tornando-a um container flex para centralizar os cards. Em seguida, estilizar a classe `product-card`, adicionando uma borda, padding e um alinhamento para seus elementos internos. (CONCLUÍDA)
- [ ] Preencher o `index.html` com o esqueleto do HTML necessário (header, main, footer) e adicionar exemplos de `product-card` dentro de um `product-list` para testar os estilos.
- [ ] Implementar a lógica de carregamento de produtos dinamicamente no `index.html` usando JavaScript.
- [ ] Adicionar interatividade aos cards de produto (ex: botão de adicionar ao carrinho, detalhes do produto).