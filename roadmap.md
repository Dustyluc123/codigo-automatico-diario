
[DOC] - Tarefa Concluída:**Roadmap do Projeto:**

1.  **Criação da Estrutura HTML Básica:**
    *   Criar o arquivo `index.html` com a estrutura básica (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`). - **CONCLUÍDA**
    *   Adicionar um cabeçalho (`<header>`) com um menu de navegação (`<nav>`). - **CONCLUÍDA**
    *   No arquivo `index.html`, crie as seções principais do corpo da página abaixo do cabeçalho: 'Início', 'Produtos', 'Sobre Nós' e 'Contato'. Utilize a tag `<section>` para cada uma, atribuindo IDs correspondentes (ex: `id='inicio'`, `id='produtos'`). Adicione um título `<h2>` e um parágrafo de texto placeholder em cada seção. Por fim, atualize os links (`<a>`) na navegação do cabeçalho para que seus atributos `href` apontem para os IDs das seções correspondentes (ex: `href='#produtos'`). - **CONCLUÍDA**

2.  **Próxima Tarefa:**
    *   Adicionar um arquivo `style.css` e vinculá-lo ao `index.html`. Criar regras CSS básicas para estilizar o cabeçalho e as seções (cores de fundo, espaçamento, tipografia) para melhorar a legibilidade e a aparência inicial da página.