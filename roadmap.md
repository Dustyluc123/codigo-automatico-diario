
[DOC] - Tarefa Concluída:## Roadmap do Projeto E-commerce

### FASE 1: Estrutura Base
- [ ] Criar o arquivo `index.html` com estrutura básica.
- [X] Adicionar um elemento `<header>` acima da navegação com um título `<h1>` (ex: "Meu E-commerce") e um subtítulo `<p>`. Em seguida, no `style.css`, estilize o cabeçalho (`header`) para ter um `text-align: center`, um `background-color` e `padding` para que se destaque.
- [ ] Estilizar a navegação principal (nav) para ter um layout flexível e links estilizados.
- [ ] Criar a seção de produtos.
