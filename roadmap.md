
[DOC] - Tarefa Concluída:## Roadmap do Projeto Web

### Fase 1: Estrutura Básica e Estilização Inicial

- [X] Configurar a estrutura HTML básica no arquivo `index.html`. Isso deve incluir a declaração `<!DOCTYPE html>`, as tags `<html>`, `<head>`, e `<body>`. Dentro do `<head>`, adicione as metatags essenciais (charset, viewport), um título para a página e certifique-se de que o arquivo `style.css` está corretamente vinculado com a tag `<link>`. Dentro do `<body>`, adicione as tags semânticas principais como `<header>`, `<main>`, e `<footer>` para definir as seções principais da página. **CONCLUÍDA**
- [ ] Implementar a estrutura básica do cabeçalho (`<header>`) com navegação e logotipo.