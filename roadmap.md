
[DOC] - Tarefa Concluída:## Roadmap de Desenvolvimento

### Tarefas
- [X] Crie o arquivo `index.html`. Adicione a estrutura HTML básica, incluindo `<!DOCTYPE html>`, `<html>`, `<head>` e `<body>`. Dentro do `<head>`, inclua a tag `<meta charset='UTF-8'>`, um `<title>` para a página e a tag `<link>` para conectar o arquivo `style.css`. - **CONCLUÍDA**
- [ ] Criar o arquivo `style.css` e adicionar estilos básicos para o body.