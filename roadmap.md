
[DOC] - Tarefa Concluída:1.  **CRIAR:** O arquivo `style.css` na raiz do projeto com CSS Reset, layout principal e navegação básica. - **CONCLUÍDA**
2.  **CRIAR:** O arquivo `index.html` na raiz do projeto, incluindo a estrutura básica de um documento HTML5, o link para o `style.css` e os elementos `header`, `nav`, `main` e `footer` com conteúdo placeholder.