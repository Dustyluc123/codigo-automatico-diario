
[DOC] - Tarefa Concluída:## Roadmap de Desenvolvimento do Projeto

### Fase 1: Estrutura Base (index.html)

- [x] Adicionar um cabeçalho `<header>` ao `index.html`. Dentro do cabeçalho, crie uma barra de navegação `<nav>` contendo uma lista não ordenada `<ul>` com links `<a>` para 'Início' (`#`), 'Produtos' (`#`) e 'Contato' (`#`). - **CONCLUÍDA**

### Próximas Tarefas:

- [ ] Adicionar um arquivo `style.css` e vincular ao `index.html`. Em seguida, aplicar estilos básicos à barra de navegação para remover bullet points e alinhar os itens horizontalmente.