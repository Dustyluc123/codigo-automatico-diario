
[DOC] - Tarefa Concluída:# Roadmap do Projeto

## Configuração do Aplicativo 'pedidos'
- [X] Crie o arquivo `pedidos/urls.py` para gerenciar as rotas específicas do aplicativo 'pedidos'. Defina um `app_name = 'pedidos'` e uma lista `urlpatterns` inicial, que pode estar vazia por enquanto. Lembre-se também de incluir este novo `urls.py` no arquivo de URLs do projeto principal para que as rotas se tornem acessíveis.
- [ ] Integre o `pedidos/urls.py` no arquivo de URLs do projeto principal (e.g., `meuprojeto/urls.py`).
- [ ] Crie o arquivo `pedidos/views.py` e defina as views iniciais para o aplicativo 'pedidos' (ex: lista de pedidos, detalhe do pedido).
- [ ] Adicione as primeiras rotas em `pedidos/urls.py` para as views de lista e detalhe de pedidos.