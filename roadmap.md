
[DOC] - Tarefa Concluída:## Roadmap do Projeto

- [x] Crie o arquivo `index.html` na raiz do projeto. Adicione a estrutura HTML básica (doctype, html, head, body) e vincule o arquivo `style.css` já existente usando a tag `<link>` dentro do `<head>`. (CONCLUÍDA)
- [ ] Adicione um título apropriado à página (dentro da tag `<title>`) e um cabeçalho `<h1>` com o texto "Bem-vindo à Minha Aplicação!" dentro do `<body>`.