
[DOC] - Tarefa Concluída:## Roadmap do Projeto

- [x] Crie o arquivo `index.html` com a estrutura básica de um documento HTML5. No `<head>`, adicione a tag `<link>` para conectar o arquivo `style.css` existente. Dentro do `<body>`, crie uma seção `<main>` e, dentro dela, uma `div` com a classe `product-grid`. Adicione pelo menos três elementos `div` com a classe `product-card` dentro da `product-grid`. Cada `product-card` deve conter uma imagem (`<img>` com um `src` de placeholder), um título (`<h2>`) e um preço (`<p>`) com conteúdo de exemplo. (CONCLUÍDA)
- [ ] Crie o arquivo `style.css` e adicione estilos básicos para o `body`, a `product-grid` e os `product-card`s para exibir os produtos em um layout responsivo, utilizando CSS Grid ou Flexbox.