Tarefas a fazer
- Criar a seção de produtos no `index.html` (com um container vazio para os produtos, por exemplo) e usar JavaScript (`script.js`) para carregar e exibir dinamicamente os produtos do array `products`. Para cada produto, deve-se criar um card com a imagem, nome, preço e, opcionalmente, uma breve descrição.

Tarefas Feita
- Crie o arquivo HTML principal (index.html) para o e-commerce. Inclua a estrutura básica de um documento HTML5: doctype, tags html, head e body. Dentro do head, adicione um título significativo para a loja e metatags essenciais como charset e viewport. No body, adicione um cabeçalho simples (h1) com o nome da loja para indicar que a página está funcionando. Este será o ponto de partida para a interface do usuário. (CONCLUÍDA)
- Adicione estilos CSS básicos para o arquivo index.html, criando um arquivo style.css. Conecte-o ao HTML. Implemente um layout inicial para o cabeçalho e, opcionalmente, um rodapé simples. (CONCLUÍDA)
- Crie o arquivo JavaScript principal (`script.js`) para a lógica do lado do cliente. Conecte-o ao `index.html` antes do fechamento da tag `</body>` para garantir que o DOM esteja carregado ao executar o script. Dentro de `script.js`, adicione um `console.log("Script principal carregado!");` para verificar a conexão. Em seguida, defina um array JavaScript simples de objetos de produto (com propriedades como `id`, `name`, `price`, `image`, `description`) para simular dados de produtos que serão usados posteriormente para exibição. (CONCLUÍDA)

Lista de Todos os arquivos ja crido
- index.html
- style.css
- script.js