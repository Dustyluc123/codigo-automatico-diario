
[DOC] - Tarefa Concluída:- [ ] Configurar o ambiente Django e Django REST Framework.
- [ ] Criar os modelos `Produto` e `CategoriaProduto` em `produtos/models.py`.
- [ ] Criar os serializers `ProdutoSerializer` e `CategoriaProdutoSerializer` em `produtos/serializers.py`.
- [X] Crie as ViewSets para `Produto` e `CategoriaProduto` em `produtos/views.py`.
- [ ] Configurar as URLs da API em `produtos/urls.py` e `projeto_raiz/urls.py` para as ViewSets criadas.
- [ ] Implementar autenticação e permissões (se necessário).
- [ ] Adicionar testes para os modelos, serializers e ViewSets.