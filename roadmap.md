
[DOC] - Tarefa Concluída:1. Tarefa: Configurar o ambiente Django e DRF. (CONCLUÍDA)
2. Tarefa: Definir os modelos `Produto` e `CategoriaProduto` em `produtos/models.py`. (CONCLUÍDA)
3. Tarefa: Implementar o `ProdutoSerializer` em `produtos/serializers.py` usando `ModelSerializer` do DRF, incluindo todos os campos e o relacionamento com `CategoriaProduto`. (CONCLUÍDA)
4. Próxima Tarefa: Criar as ViewSets para `Produto` e `CategoriaProduto` em `produtos/views.py` usando `ModelViewSet` para fornecer operações CRUD completas via API.