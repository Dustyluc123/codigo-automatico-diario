
[DOC] - Tarefa Concluída:- [x] No arquivo `index.html`, crie a seção principal. Adicione uma tag `<main>` logo abaixo do `<header>` e inclua um `<h1>` com o título "Bem-vindo à Nossa Aplicação" e um parágrafo de texto descritivo.
- [ ] Próxima Tarefa: Implementar a navegação principal no `<header>` do `index.html`, incluindo links para as seções principais da aplicação.