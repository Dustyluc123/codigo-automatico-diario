
[DOC] - Tarefa Concluída:## Roadmap do Projeto

- [ ] Configurar o ambiente Django.
- [ ] Criar o projeto Django 'meuprojeto'.
- [ ] Criar o app 'pedidos'.
- [X] Dentro do diretório do app 'pedidos', crie um novo arquivo chamado `urls.py`. Neste arquivo, importe `path` de `django.urls` e `views` do diretório local (do app 'pedidos'). Em seguida, defina uma lista `urlpatterns` que mapeie o caminho raiz ('') para a view `views.index`, nomeando essa rota como 'index'.
- [ ] Criar o arquivo `pedidos/views.py` e definir a função `index` que retorna um `HttpResponse` simples.
