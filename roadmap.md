
[DOC] - Tarefa Concluída:## Roadmap do Projeto E-commerce

### Sprint 1: Estrutura Base e Seções Principais

- [X] Criar estrutura básica do `index.html` e adicionar seções `hero` e `produtos-destaque`.
  - **Status:** CONCLUÍDA
  - **Detalhes:** O arquivo `index.html` foi criado do zero, incluindo a estrutura HTML5 padrão, as tags `<header>`, `<main>` e `<footer>`. A tag `<main>` foi adicionada após o `<header>` e contém as seções `<section id='hero'>` e `<section id='produtos-destaque'>`, cada uma com um `<h2>` como placeholder.

### Próxima Tarefa:

- [ ] Adicionar conteúdo detalhado e estilos iniciais para o cabeçalho (ex: logo, barra de navegação completa, ícone de carrinho).