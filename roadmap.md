INICIO_DO_PROJETO

### Tarefas a fazer
- [ ] Adicionar estilos básicos ao `css/style.css` para o layout inicial da página (cabeçalho, navegação, conteúdo principal e rodapé).

### Tarefas Feita
- [x] Crie o arquivo `index.html`, que servirá como a página inicial e o ponto de entrada principal para a aplicação de e-commerce. Ele deve conter a estrutura HTML5 básica, um título relevante para a loja e placeholders para futura inclusão de arquivos CSS e JavaScript. (Adicionado conteúdo inicial com estrutura HTML5 básica para uma aplicação genérica, incluindo header, main e footer, com título "Minha Aplicação". O foco em e-commerce será abordado em etapas futuras.)
- [x] Crie a estrutura de diretórios `css/` e `js/` na raiz do projeto. Dentro de `css/`, crie o arquivo `style.css` (vazio por enquanto). Dentro de `js/`, crie o arquivo `main.js` (vazio por enquanto).
- [x] Integre os arquivos CSS e JavaScript criados anteriormente ao `index.html`. Adicione a tag `<link>` para o `css/style.css` dentro do `<head>` e a tag `<script>` para o `js/main.js` antes do fechamento da tag `</body>`. (Tags de link e script adicionadas ao `index.html` para `css/style.css` e `js/main.js`.)

### Lista de Todos os arquivos ja crido
- `index.html`
- `css/style.css`
- `js/main.js`