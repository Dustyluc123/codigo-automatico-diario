
[DOC] - Tarefa Concluída:# Roadmap do Projeto de Loja Online

## FASE 1: Estrutura Base e Estilização Inicial

- [X] Crie o arquivo `index.html`. Adicione a estrutura HTML5 básica (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`). No `<head>`, inclua a tag `<title>` para o nome da loja e a tag `<link>` para conectar o arquivo `style.css` já existente. No `<body>`, adicione uma estrutura semântica para o cabeçalho (`<header>`) e a navegação principal (`<nav>`), preparando a base para os próximos componentes. - **CONCLUÍDA**

## PRÓXIMA TAREFA:

- [ ] Adicione o conteúdo principal ao `index.html`. Crie uma seção principal (`<main>`) e, dentro dela, inclua uma `<section>` para um banner ou hero section e outra `<section>` para exibir uma grade de produtos em destaque.