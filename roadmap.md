
[DOC] - Tarefa Concluída:### Roadmap do Projeto Loja Online

1.  **Estrutura Básica do HTML e Cabeçalho**
    *   Criar o arquivo `index.html` com a estrutura básica.
    *   Dentro da tag `<header>`, adicionar o logotipo (`<img>`) e o título principal (`<h1>`).
    *   Status: **CONCLUÍDA**

2.  **Definição da Navegação Principal**
    *   Dentro da tag `<header>`, abaixo do título, adicionar uma tag `<nav>` contendo uma lista não ordenada (`<ul>`) de links de navegação (`<li><a>`) para as seções principais da loja (ex: 'Início', 'Produtos', 'Categorias', 'Contato').