
[DOC] - Tarefa Concluída:## Roadmap de Desenvolvimento Frontend

### Etapa 1: Estrutura HTML Principal

- [x] Tarefa 1.1: Criar o arquivo `index.html` com a estrutura básica (`<!DOCTYPE>`, `<html>`, `<head>`, `<body>`) e adicionar um `<header>` com navegação simples. (Concluída implicitamente para esta tarefa)
- [x] Tarefa 1.2: Abaixo da tag `<header>` existente no arquivo `index.html`, adicione uma tag `<main>`. Dentro da `<main>`, crie uma `<section>` que servirá como o banner principal. Esta seção deve conter um título `<h2>` com o texto 'Bem-vindo à Nossa Loja!', um parágrafo `<p>` com uma breve descrição como 'As melhores ofertas, você encontra aqui.' e um botão `<button>` com o texto 'Ver Produtos'. (CONCLUÍDA)

### Próximos Passos

- [ ] Tarefa 1.3: Adicionar uma seção de 'Produtos em Destaque' abaixo do banner principal, com uma grade de produtos e placeholders para imagens e descrições.