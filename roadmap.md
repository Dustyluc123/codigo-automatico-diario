
[DOC] - Tarefa Concluída:## Roadmap do Projeto

### Próximas Tarefas

1. No arquivo `index.html`, dentro da `div` com a classe `product-list`, crie a estrutura HTML para um único card de produto. O card deve ser uma `div` com a classe `product-card` e conter: uma imagem do produto (`img`), o nome do produto (`h3`), o preço (`p`), e um botão 'Adicionar ao Carrinho' (`button`). Use placeholders para o conteúdo por enquanto. - CONCLUÍDA

2. Crie um arquivo `style.css` e adicione estilos básicos para o layout da página, incluindo a centralização do `product-list` e o estilização do `product-card` com borda, padding e um layout flexível para seus elementos internos.