
[DOC] - Tarefa Concluída:## Roadmap do Projeto

### Fase 1: Configuração Inicial e Componentes Básicos

- [X] Criar o componente `Home` para a página inicial em `src/pages/Home.jsx`.
  - Defina um componente React funcional `Home`.
  - Retorne uma estrutura JSX com um título principal (`<h1>`) e um parágrafo introdutório.
  - Exporte o componente `Home` como padrão.
  (CONCLUÍDA)

### Próximas Tarefas:

- Criar o componente `Header` para a navegação global.
  - Incluir um logotipo e links de navegação (Home, Produtos, Contato).
  - Integrar estilos básicos.
- Criar o componente `Footer` para informações de rodapé.
  - Incluir informações de copyright e links de redes sociais.
- Configurar o roteamento básico da aplicação (React Router DOM).
  - Definir rotas para Home, Produtos, e outras páginas futuras.
- Criar a página `Products` para exibir uma lista de produtos.
  - Desenvolver um componente `ProductCard` para cada produto.
